import type { SherpaTheme } from '@sherpa/types';

export const DEFAULT_THEME: SherpaTheme = {
  overlay: {
    color: '#000000',
    opacity: 0.6,
  },
  tooltip: {
    backgroundColor: '#FFFFFF',
    textColor: '#000000',
    borderRadius: 8,
    padding: 16,
    titleFontSize: 16,
    bodyFontSize: 14,
  },
  beacon: {
    color: '#FFFFFF',
    opacity: 0.7,
  },
};
