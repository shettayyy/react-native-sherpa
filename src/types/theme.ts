export type SherpaTheme = {
  overlay: {
    color: string;
    opacity: number;
  };
  tooltip: {
    backgroundColor: string;
    textColor: string;
    borderRadius: number;
    padding: number;
    titleFontSize: number;
    bodyFontSize: number;
  };
  beacon: {
    color: string;
    opacity: number;
  };
};
